// The app currently uses the root `src/` directory (see `vite.config.js` + `tsconfig.json`).
// This file exists to match older repo layouts that referenced `client/src/...`.
export { default } from "../../../src/pages/CoachPage";

